# Auto-detect text files and normalize line endings in the repository
* text=auto

# Force LF for common text-based files so Git stores them with LF in repo
*.java text eol=lf
*.xml text eol=lf
*.properties text eol=lf
*.yml text eol=lf
*.yaml text eol=lf
*.md text eol=lf
*.gradle text eol=lf
*.sh text eol=lf
.vscode/* text eol=lf

# Treat log files as binary (don't try to normalize their line endings)
*.log binary

# Prevent accidentally changing executable bits
*.exe binary
